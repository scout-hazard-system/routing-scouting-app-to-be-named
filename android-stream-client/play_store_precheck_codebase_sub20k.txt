PLAY STORE PRE-CHECK COMPACT (SUB-20K)
Current codebase snapshot (selected high-signal lines).

=== FRONTEND (ANDROID) ===
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="androidx.car.app.NAVIGATION_TEMPLATES" />
    <uses-permission android:name="androidx.car.app.ACCESS_SURFACE" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.type.automotive" android:required="false" />
    <uses-feature android:name="android.software.car.templates_host" android:required="false" />
        <service
            android:name=".car.StreamCarAppService"
            android:foregroundServiceType="location">
                <action android:name="androidx.car.app.CarAppService" />
    namespace "dev.warp.stream"
    compileSdk 34
        applicationId "dev.warp.stream"
        minSdk 26
        targetSdk 34
        versionCode 1
        versionName "0.1.0"
    flavorDimensions "appmode"
            dimension "appmode"
            applicationIdSuffix ".client"
            versionNameSuffix "-client"
    implementation project(":frontend-ui")
    implementation "androidx.appcompat:appcompat:1.7.0"
    implementation "com.google.android.material:material:1.12.0"
    implementation "androidx.core:core-ktx:1.13.1"
    implementation "com.squareup.okhttp3:okhttp:4.12.0"
    implementation "androidx.car.app:app:1.4.0"
  private static final long DEVICE_GPS_POST_INTERVAL_MS = 3000L;
  private static final long SERVER_ROUTE_REFRESH_MS = 5000L;
  private static final long POPUP_AUTO_HIDE_MS = 12000L;
  private static final boolean ENABLE_DEV_CONTROLS = BuildConfig.ENABLE_DEV_CONTROLS;
    addressCatalogRouter.suggest(
    openRouteOptionsScreen(candidate.lat, candidate.lon, candidate.displayName);
    addressCatalogRouter.resolve(
  private void openRouteOptionsScreen(double destLat, double destLon, String destLabel) {
            + "/api/map/scene?lat="
      locationManager.requestLocationUpdates(
    openRouteOptionsScreen(targetLat, targetLon, "Map target");
            .url(base + "/api/gps/update")
            + "/api/platform/route/local"
  interface SuggestCallback {
  void resolve(
            + "/api/platform/address-catalog/resolve?q="
        baseUrl + "/api/platform/geocode?q=" + Uri.encode(query) + biasParams(biasLat, biasLon);
  void suggest(
            + "/api/platform/address-catalog/suggest?q="
              .url(baseUrl + "/api/platform/address-catalog/upsert")
    <string name="app_name">Traffic Pulse Pathfinder</string>
    <string name="destination_hint">Search destination address</string>
    <string name="search_go">Route</string>
    <string name="popup_vet_pass">✅ Vetted alert</string>
    <string name="popup_vet_fail">⚠️ Unvetted alert</string>
    <string name="osm_odbl_notice">Map data © OpenStreetMap contributors (ODbL 1.0). Dataset: https://planet.openstreetmap.org  License: https://opendatacommons.org/licenses/odbl/1-0/  This app reads OSM data in read-only mode and does not modify upstream OSM records.</string>

=== BACKEND (JAVA SERVER) ===
  private static final String NOMINATIM_SEARCH_URL =
      System.getenv().getOrDefault("NOMINATIM_SEARCH_URL", "https://nominatim.openstreetmap.org/search");
  private static final double GEOCODE_BIAS_RADIUS_DEGREES =
      parseDouble(System.getenv("GEOCODE_BIAS_RADIUS_DEGREES"), 0.35);
  private static final int SUGGEST_DEFAULT_LIMIT =
      parseIntOrDefault(System.getenv("ADDRESS_SUGGEST_DEFAULT_LIMIT"), 8);
  private static final int SUGGEST_MAX_LIMIT =
      parseIntOrDefault(System.getenv("ADDRESS_SUGGEST_MAX_LIMIT"), 12);
  private static final String[] SCOUT_MODELS = {"scout-core1.0.2", "scout-rank"};
    registerContext(server, "/api/platform/route/local", new LocalRouteHandler());
    registerContext(server, "/api/platform/geocode", new GeocodeHandler());
    registerContext(server, "/api/platform/address-catalog/suggest", new AddressCatalogSuggestHandler());
    registerContext(server, "/api/gps/update", new GpsUpdateHandler());
    registerContext(server, "/api/map/scene", new MapSceneHandler());
  private static String buildAddressCatalogSuggestJson(Map<String, String> query) {
    int limit = parseIntOrDefault(query.getOrDefault("limit", ""), SUGGEST_DEFAULT_LIMIT);
    if (limit > SUGGEST_MAX_LIMIT) {
      limit = SUGGEST_MAX_LIMIT;
        String baseUrl = NOMINATIM_SEARCH_URL + "?format=json&limit=5&q=" + urlEncode(q);
              trimDouble(biasLon - GEOCODE_BIAS_RADIUS_DEGREES)
                  + trimDouble(biasLat - GEOCODE_BIAS_RADIUS_DEGREES)
                  + trimDouble(biasLon + GEOCODE_BIAS_RADIUS_DEGREES)
                  + trimDouble(biasLat + GEOCODE_BIAS_RADIUS_DEGREES);
  private static String buildAddressCatalogResolveJson(Map<String, String> query) {
  private static final class AddressCatalogSuggestHandler implements HttpHandler {
  private static final class GeocodeHandler implements HttpHandler {
    for (int i = 0; i < SCOUT_MODELS.length; i++) {
      boolean present = installed.contains(SCOUT_MODELS[i]);
      models.append("\"").append(SCOUT_MODELS[i]).append("\":").append(present);

=== GRADLE/APK BUILD ===
    id("com.android.application") version "8.6.1" apply false
    id("com.android.library") version "8.6.1" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
rootProject.name = "ScannerStreamClient"
include(":app")
include(":frontend-ui")
    namespace "dev.warp.stream"
    compileSdk 34
        applicationId "dev.warp.stream"
        minSdk 26
        targetSdk 34
        versionCode 1
        versionName "0.1.0"
    flavorDimensions "appmode"
            dimension "appmode"
            applicationIdSuffix ".client"
            versionNameSuffix "-client"
    implementation project(":frontend-ui")
    implementation "androidx.appcompat:appcompat:1.7.0"
    implementation "com.google.android.material:material:1.12.0"
    implementation "androidx.core:core-ktx:1.13.1"
    implementation "com.squareup.okhttp3:okhttp:4.12.0"
    implementation "androidx.car.app:app:1.4.0"
