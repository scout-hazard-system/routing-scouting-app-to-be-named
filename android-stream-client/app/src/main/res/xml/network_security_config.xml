<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev build talks to a local HTTP backend, so cleartext must be allowed. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
